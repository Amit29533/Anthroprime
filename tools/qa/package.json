{
  "name": "anthroprime-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Static + runtime QA harness for the AnthroPrime static site. The site itself is zero-build; these tools only verify it.",
  "type": "module",
  "scripts": {
    "audit": "node audit.mjs",
    "test": "node interactions.mjs",
    "qa": "node audit.mjs && node interactions.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
